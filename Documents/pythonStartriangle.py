String symbol = "*"

        for (int count = 1; count <= rows; count++) 
            for (int counter = 1; counter <= count; counter++) 
                          print(symbol + " ")
            
                       println(] 